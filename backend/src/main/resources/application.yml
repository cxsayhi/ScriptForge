server:
  port: 8080

spring:
  application:
    name: scriptforge-backend
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 12MB

scriptforge:
  storage:
    root: ../storage
