/**
 * MyBatis mapper interfaces live here.
 *
 * PR-A1 only prepares the package and scan configuration. Concrete mappers are
 * added when the in-memory services are migrated to MySQL.
 */
package com.scriptforge.novelscript.mapper;
